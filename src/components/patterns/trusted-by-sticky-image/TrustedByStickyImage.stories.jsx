import { TrustedByStickyImage } from './TrustedByStickyImage';

export default {
  title: 'Patterns/Testimonials',
  parameters: { layout: 'fullscreen' },
};

/* The "Kind Words" light multi-column carousel — the version used on CMS Page V2. */
export const Default = () => <TrustedByStickyImage variant="cards" />;
